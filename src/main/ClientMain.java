package main;import DiceView.ClientFrame;import client.*;import common.*;public class ClientMain implements ChatIF {	// Class variables *************************************************	//final public static int DEFAULT_PORT = 5555;	ClientState client;	// Constructors ****************************************************	public ClientMain(String host, int port, String login) {		client = new ClientState(host, port, login, this);	}	public ClientState getClient() { //ClientState를 리턴해주넹		return client;	}	public void setClient(ClientState client) {		this.client = client;	}	public boolean accept() {		try {			//client.My_login();			//ClientFrame.getInstance().chat.getClient().handleMessageFromClientUI("연결");			return true;				} catch (Exception ex) {					System.out.println("Unexpected error while reading from console!");			return false;		}	}	public void display(String message) {		System.out.println(message);	}		// Class methods ***************************************************	public static void main(String[] args) {			ClientFrame.getInstance().setVisible(true);	}}// End of ConsoleChat class